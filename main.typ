// ════════════════════════════════════════════════════════════
//  A4 三栏紧凑型 · 考试开卷速查模板 (Typst)
//  ─ 改密度: 调下方 base-size  (7pt → 6pt / 5.5pt 更密)
//  ─ 改栏数: 改 page 的 columns: 3  (可改 2 或 4)
//  ─ 改页边距: 改 page 的 margin: 5mm
//  ─ 导出:  typst compile cheatsheet.typ
//          或直接把本文件粘到 https://typst.app 在线编译
// ════════════════════════════════════════════════════════════
#let base-size = 6.8pt                 // ← 全局基础字号，最常调的就是这个
#let accent  = rgb("#2563eb")          // 主题色(标题块/小标题)
#let accent2 = rgb("#dc2626")          // 强调色(重点高亮)
#let softbg  = rgb("#eef2ff")          // 浅底色(提示框)
#let grayln  = rgb("#cbd5e1")

#set text( //全局设置
  font: ("Noto Sans CJK SC", "Liberation Sans", "DejaVu Sans"),  // 中英字体+回退
  size: base-size,
  lang: "zh",
)
#set page(paper: "a4", margin: 5mm, columns: 3)        // ← 三栏 + 5mm 极窄边距
#set par(justify: true, leading: 0.45em, spacing: 0.5em)  // 行距/段距压紧
#set list(indent: 3pt, spacing: 0.4em, marker: text(fill: accent)[▪])
#set enum(indent: 3pt, spacing: 0.4em)
#show raw: set text(size: base-size - 0.3pt, font: ("DejaVu Sans Mono", "Noto Sans CJK SC"))

// ───────── 可复用组件 ─────────
// 带底色的分区标题
#let sec(t) = block(
  width: 100%, fill: accent, inset: (x: 4pt, y: 2.5pt),
  radius: 2pt, above: 7pt, below: 3pt,
  text(fill: white, weight: "bold", size: base-size + 0.6pt, t),
)
// 二级小标题(省空间，无底色)
#let sub(t) = text(fill: accent, weight: "bold", t)
// 重点高亮
#let hl(t)  = text(fill: accent2, weight: "bold", t)

//表格相关
#let tb(..a) = table(inset: 2pt, stroke: 0.4pt + grayln, ..a)
#let th(t) = text(weight: "bold", fill: accent, t)

//提示框
#let note(t) = block(width: 100%, fill: softbg, inset: 3pt, radius: 2pt, below: 3pt, t)

#sec[ch3软件过程模型]
#note[软件过程是为建造高质量软件所需完成的任务的框架，即形成软件产品的一系列步骤，包括中间产品、资源、角色以及过程中采取的方法、工具等范畴]

#sub[过程]是活动的集合，活动包括任务、工件（交付物）、质量保证点（要求）、里程碑

#sub[三组活动]：基本软件开发活动（需求分析、软件设计、代码编写、软件测试、软件交付、软件进化）、软件工程过程框架活动（沟通、策划、建模、构建、部署）、保护性活动（项目跟踪和控制、风险管理、质量保证、技术评审、软件度量、软件配置管理、复用管理、工作产品的准备和生产）

#sub[经典软件过程模型]

#tb(columns: (auto, 1fr),
  th[模型], th[要点 / 适用],
  [瀑布], [线性、文档驱动；需求#hl[明确稳定]，质量要求高],
  [原型], [快速原型探需求；需求#hl[不明确]],
  [增量], [需求较为清晰，功能并行、先做高风险，分批交付、核心先行；可分模块；有严格交付期限、资源有限],
  [迭代], [小瀑布的集合，短反馈、风险控制、管理要求高，需求持续变更、高风险，先做出来后优化],
  [演化], [#hl[迭代式开发+增量交付]，需求不确定、创新/探索型，逐步添加功能],
  [螺旋], [瀑布+原型+迭代+#hl[风险分析]；大型高风险],
  [RUP], [#hl[用例驱动]、以架构为中心、迭代增量],
)
- #sub[RUP 四阶段]：初始→细化→构造→移交。
- 只有瀑布模型和增量模型是应对清晰的需求的，别的全是不清晰，清晰的需求用户参与度就低，别的中高
- 增量交付：可以开发完核心功能后快速上线，迭代式开发：可以吸收经验应用到下一个迭代中

#sec[ch4软件开发方法]

#sub[结构化开发方法：]更适合清晰稳定的需求，但是变化易在全局传播，一开始设计数据流，后续又要拆成模块，有鸿沟

#sub[面向对象开发方法：]适合大型、复杂、需长期维护的系统，面向对象分析和面向对象设计过度平滑

#sub[敏捷开发方法: ]#hl[原则：]尽早且持续交付有价值的产品，欢迎变更，业务人员与开发人员在一起工作，面对面交谈，可用作为指标，简洁；#hl[价值观：]个体与互动＞过程工具；可运行软件＞详尽文档；客户合作＞合同谈判；响应变化＞遵循计划

典型的敏捷开发方法：
- 极限编程XP（特色：结对编程）：沟通，简单，反馈（测试），尊重，勇气
- Scrum方法：若干迭代周期组成，每个周期称为冲刺，产品经理将产品分解为有优先级的产品任务列表->取出一定任务作为一次冲刺（此时冻结，需求不能变了）->开站会等->这个冲刺结束之后再循环下一个冲刺

为什么OO方法#hl[填平]了结构化方法的两道#hl[鸿沟]：
+ 封装把操作和数据绑定，填平“数据/行为鸿沟”
+ OOA与OOD用一致的表示法，无需大规模转换，填平了“分析/设计鸿沟”

有人认为软件工程浪费时间，#hl[敏捷]的开发出代码才最重要？

+ 软件工程恰恰是为了解决"只写代码"的失败而诞生的。
+ "能运行的代码"≠ 正确、可靠、可维护的软件。（要有需求分析、设计以及质量测试等）
+ 省略前期工作的代价会在后期指数级放大。
+ 文档与计划支撑协作、维护与知识传承。
+ 把敏捷等同于"只写代码"，是对敏捷的根本误读。（敏捷是有计划、过程、纪律性工程实践、设计以及必要的文档的，只是反对官僚的、过度繁琐的流程和无效的文档）

关于#hl[敏捷开发]意味着可以不需要文档、设计和计划：
+ 计划：敏捷计划的更频繁，比如上面Scrum的流程，XP的计划游戏等。 可见敏捷不是"不计划"，而是把"一次性详尽计划"换成了滚动式、分层、频繁重规划——计划的频率反而更高。
+ 设计：敏捷做的是"持续设计"，而非"不设计"。XP包含简单设计，隐喻，CRC卡片等设计技术，以及持续重构。反对的是大而全的前期设计，主张演进式设计。
+ 文档：敏捷保留"刚好够用 + 活文档"。比如TDD的测试，产品代办列表本身

#sec[ch2软件工程概述]

#note[软件工程是一门研究如何用系统化、规范化、可量化等工程原则和方法进行软件开发和维护的学科。其目标是创建“足够好”、用户满意的软件。不仅关注软件开发的技术过程，也关注软件项目管理以及支持软件开发的工具、方法和理论]

#hl[七条原则：]1.使用分阶段的生命周期计划进行管理2.坚持进行阶段评审3.实行严格的产品控制4.采用现代编程实践5.对结果保持明确的问责制6.开发团队应该少而精7.软件过程持续改进

#hl[软件危机：]#sub[表现：]1.所交付软件不符合用户的需求2.软件质量不可靠，得不到用户的信赖3.项目超预算或延误。#sub[根源：]1.软件复杂度2.软件开发技术问题3.组织和管理问题4.市场需求问题（需求变化快，推着对开发要求也更快）5.缺乏标准和规范

#hl[计算机辅助软件工程的工具：]辅助软件工程全生命周期的开发工具和方法集合1.系统分析与设计工具（UML）2.程序设计工具（IDEA）3.软件测试与质量保证工具（Java压测工具JMeter）4.软件项目管理工具（GitHub）5.软件运维工具（Docker）

#hl[软件工程三要素：]过程（管理视角）、方法学（技术视角）、工具（辅助视角）

#sec[ch1软件]

#note[
  软件=程序+数据+文档\
  特点：复杂性、一致性、易变性和不可见性
]

#sub[分类：]系统软件（编译器和硬件驱动也属于这类）和应用软件（为了某种用途被开发）
另一种分类方式：关键软件：关键基础软件（os、db、中间件、办公）、大型工业软件、行业应用软件（金融）、新型平台软件、嵌入式软件

#sub[软件生存周期(SLC)：]软件开发生存周期(SDLC)+交付后的维护阶段。软件产品生存周期：包含多个SDLC，持续改进和增强。阶段：
+ 软件定义时期：问题定义、可行性研究、需求分析
+ 软件开发时期：总体设计、详细设计、编码和单元测试、综合测试
+ 软件运行维护时期（工作量占比最大）

#sub[软件质量模型：]（质量衡量标注）
+ McCall模型：3个维度11个质量因素，目的是避免开发用户不想要的模型，产品运行：正确性、可靠性、效率、安全性、可用性；产品修改：可维护性、灵活性、可测试性；产品转移：可移植性、可重用性、互操作性
+ Garvin模型：8个质量维度，目的是"主动提供用户真正想要的产品"，性能、特性、可靠性、符合性、耐用性、服务性、美学性、感知质量
+ ISO/IEC 25010：8大特性、31个子特性，现行国际标准，最系统最完整，功能适合性、性能效率、兼容性、易用性、可靠性、信息安全性、维护性、可移植性

#sub[软件质量保证技术(SQA)：]

静态（无需运行程序，尽早发现问题、成本低）：

- ① 质量标准制定：编程/设计/测试/文档等规范，贯穿全程
- ② 阶段评审：人工审查需求、设计、代码（需求评审、设计评审、代码走查）
- ③ 软件静态分析：工具自动扫描代码结构/语法/语义（如SonarQube、FindBugs）

动态（需运行程序，验证真实行为、成本较高）：

- ④ 软件测试：实际运行验证（单元/集成/系统/验收测试）
- ⑤ 代码重用：复用已验证的代码，降低引入缺陷的风险

#sec[ch8软件测试]

#note[
  + 静态测试：代码走查（问问思路）、技术评审、代码审查
  + 动态测试：1.是否关心内部实现（白盒/黑盒/灰盒测试）2.按开发过程阶段（单元/集成/系统/验收/回归测试）3.按实施组织：开发方测试（$alpha$测试）、用户测试（$beta$测试）、第三方测试
]

#sub[软件测试的七条准则：]1.测试可以证明缺陷存在，但不能证明不存在2.穷尽测试是不可能的3.测试活动应该尽早开始4.缺陷集群性5.杀虫剂悖论（一组测试用例一直用会变无效）6.测试依赖于测试内容7.“没有失效就是有用系统”是一种谬论

#sub[白盒测试：]（要求了解代码内部逻辑）

语句覆盖（每条语句执行一次）-> 判定覆盖（每个判定的每种结果至少一次）-> 条件覆盖（每个条件（原子）的真假至少各一次）-> 判定-条件覆盖（同时满足判定覆盖和条件覆盖）-> 条件组合覆盖（每个判定中所有组合出现一次，比如AorB，AB就要分别出现TT、TF、FT、FF）-> 路径覆盖（覆盖程序流图中的所有路径）

#hl[基本路径覆盖：]至少引入一个新语句或者新判断的程序通道

+ 程序控制流图绘制：判定点、过程块、结合点、结束点，注意判定点不应包含符合条件，如果包含需要拆开为两个点
+ 计算圈复杂度：E(dges) - N(odes) + 2，结果为基本路径数
+ 给复合条件标两个节点，括号内多句话捏成一个节点，单个条件也分配一个节点，单句话分配一个节点，#hl[终止点也有一个节点，比如while，从开始直接指向这个终止点就是一条基本路径]
+ 可以适当标注一些T和F和入口出口等辅助自己理解
+ 提到满足基本路径覆盖的用例，就标注每条路径对应的测试用例，如果有某条路径无法经过，也要对应说明

#sub[黑盒测试：]（不看内部，仅关心输入输出）

+ 等价类划分法：把输入域划分为若干集合，每个集合中只选一个，分为#hl[有效等价类]和#hl[无效等价类]。测试用例设计原则：#hl[一个用例尽量覆盖所有有效类]；每个无效类单独一个用例（#hl[每次只让一项非法]、其余保持合法）。#hl[这个等价类可以划分的细一点]
+ 边界值分析法：找正好等于，刚刚大于或刚刚小于边界的值、空值来测试
+ 错误推测法：根据经验来
+ 判定表法：分条件桩（一些条件）和动作项（结果是什么）画表

#sub[按照阶段分类：]
#[
  #table(
    columns: 6,
    align: left + horizon,
    inset: 3pt, stroke: 0.4pt + gray,
    fill: (_, r) => if r == 0 { luma(230) },
    table.header[级别][阶段][对象][人员][依据][方法],
    [单元测试], [编码后], [最小可测单元], [开发者], [代码/详细设计文档], [白盒],
    [集成测试], [单元后], [模块接口/协作], [开发者], [概要设计文档], [黑+白],
    [确认测试], [集成后(冒烟过)], [系统], [测试员+用户], [需求规格说明书], [黑盒],
    [系统测试], [确认后], [整系统(软硬件)], [测试员+用户], [系统说明书/需求文档], [黑盒],
    [验收测试], [发布前], [整个系统], [用户/需求方], [需求/验收标准], [黑盒],
  )
]

#sec[流图、用例图、顺序图、类图等]

#note[导出方式：ctrl+shift+p，export current diagram]

#sub[流图：]

`@startuml
' 为了让图形更紧凑，可以设置方向从上到下
top to bottom direction

' 定义圆形节点
usecase "1" as n1
usecase "2" as n2
usecase "3" as n3

' 绘制边和方向
n1 --> n2
n2 --> n3 : T

@enduml`

#hl[注意：]

+ 复合条件需要拆开
+ 从起始点直接到终止点的路径不要忘记

#sub[用例图：]

`@startuml 树洞APP用例图
left to right direction
actor "匿名用户" as User
actor "审核员" as Auditor
actor "用户" as UUser

rectangle 匿名树洞APP {
    usecase "匿名登录" as UC00
    usecase "发布内容" as UC10
    usecase "AI情绪疏导" as UC11
    usecase "内容审核" as UC20
}

' 泛化关系
Auditor --|> UUser
User --|> UUser

UC10 ..> UC00 : <<include>>
UC11 ..> UC10 : <<extend>>

' 如果用--就不强调角色发起用例的关系
User --> UC10
Auditor --> UC20
@enduml`

#hl[注意：]

+ 不是提到了的角色都要化成参与者，比如保洁用钥匙，不经过系统，就不需要体现在图上
+ 一些外部系统，比如：学校统一身份认证系统、短信系统也要画
+ 所有角色都要做的，比如身份认证，可以抽象出一个父角色，让他做，而不必所有人都去指一遍，泛化（子指向父）

#sub[顺序图：]

- boundary/control/entity：边界/控制/实体类
- `->`同步消息，`-->`返回消息
- `activate/deactivate`：激活/销毁生命周期
- `alt/opt/loop/par`：条件/可选/循环/并行
- `ref over`：嵌套子流程（复合顺序图）
- `autonumber`：自动编号

`@startuml 发布内容顺序图
autonumber
actor "用户" as User
boundary "发布UI" as UI <<boundary>>
control "内容控制器" as Manager <<controller>>
entity "内容实体" as Post <<entity>>

User -> UI : 提交内容
activate UI
UI -> Manager : 发起发布请求
activate Manager

' 嵌套子流程
ref over Manager, Post : 权限校验子流程

' 条件分支
alt 权限通过
    Manager -> Post : 保存内容
    Post --> Manager : 成功
    Manager --> UI : 发布成功
else 权限失败
    Manager --> UI : 发布失败
end

deactivate Manager
deactivate UI
@enduml`

`par
  ATM -> 银行系统 : 记录交易日志
else
  ATM -> 用户 : 返回主菜单
end`

`opt 用户选择打印凭条
  ATM -> 用户 : 打印凭条
end`

`loop 密码错误, 最多3次
    ATM -> Bank : 验证密码(卡号, pwd)
    activate Bank
    Bank --> ATM : 返回验证结果
    deactivate Bank
end`

#hl[注意：]
+ 预约界面，预约控制器，用户账号，会议室，预约记录，短信系统
+ 从上到下是完成某个操作随着时间的各种行为

#sub[类图：]

`@startuml
class BankAccount {
  - id : String
  - balance : double
  - {static} interestRate : double
  --
  + deposit(amount : double) : void
  + getBalance() : double
  + {abstract} calculate() : double
}
@enduml`

`#`protected, `~`package

特殊类型：抽象类、接口、枚举

`@startuml
abstract class Shape {
  + {abstract} area() : double
}

interface Drawable {
  + draw() : void
}

enum OrderStatus {
  PENDING
  PAID
  SHIPPED
  COMPLETED
}
@enduml`

六种关系

泛化/继承：子类指向父类`B --|> A`

实现：实现类指向接口`B ..|> A`

接下来关联、聚合、组合、依赖

关系强弱程度（耦合程度）：依赖`..>` < 关联`--/-->` < 聚合`o--` < 组合`*--`

组合：整体消亡，部分同步消亡

聚合：部分可以独立存在

如果偶尔传进来用一下是依赖，作为字段一直持有是关联

多重性#hl[必须标注]：写在关系线两端，表示一个对象对应另一端的数量

`Customer "1" --> "0..*" Order : places >
Teacher  "1" -- "1..*" Student : teaches`

#hl[注意：]
+ 可以直接用汉字写一些函数名和变量名
+ 可以利用上面顺序图的拆解方式直接画类图

#sec[ch9软件部署与维护]
#note[
  软件维护：软件交付使用后，对软件系统及其配套设施进行修改的过程，目的是修复缺陷、提高性能或其他属性、增强功能以及适应变化的环境。

  软件的可维护性：软件交付使用后，为满足新要求而进行有效修改、扩展和优化的能力（含修复缺陷、改进设计、增加功能、适应新环境）。
]

#sub[影响软件可维护性的因素：]1.可理解性：结构、组件、代码易于理解和解释2.可测试性：测试易进行，能检出错误3.可修改性：易修改与扩展，满足新需求4.可移植性：能在不同环境部署运行5.可重用性：具有可重用组件

#sub[软件维护的形式和类别：]

#table(
  columns: (auto, 1fr, 1.3fr, 1.4fr),
  inset: 3pt,
  align: (left + horizon, left, left, left),
  stroke: 0.4pt + gray,
  fill: (_, row) => if row == 0 { luma(230) },
  table.header(
    [*类型*], [*驱动*], [*目的*], [*关键特点*],
  ),
  [*纠正性*], [已暴露的缺陷], [修复 bug、保正常运行], [#hl[被动性]、紧急性],
  [*改善性*], [内部求更好 / 新需求], [提性能、增功能、优体验], [功能增强、性能优化、UX 提升],
  [*适应性*], [外部环境变化], [与新环境兼容], [环境适应、兼容性测试、更新频繁],
  [*预防性*], [为未来风险], [提可维护性 / 可靠性、延长寿命], [#hl[前瞻性]、持续性、系统性],
)

#sub[软件维护的任务：]旨在确保软件全生命周期保持高质量、高性能、高可用。主要任务：问题诊断、缺陷修复、功能更新、性能优化、兼容性适应、代码重构、文档更新、安全性维护、用户支持、软件测试。

#sub[软件维护的过程：]问题收集和分析、提出更改请求、设计修改方案、评审修改方案、实施修改方案、软件测试、软件发布和部署、监控和用户反馈、调整和优化

#sub[软件维护的原则：]风险可控、主体维持、系统总体结构优化、复杂性可控

#sub[软件维护的实施策略：]

建立规范的流程；分类实施（按四类维护分别处理）；强化技术支持与工具链建设；组织保障与团队协作机制；推动从"被动修复"到"主动预防"

#sub[软件维护技术：]

+ DOM 面向缺陷：目标：修缺陷、降故障率。五阶段：识别→分类→定位→修复→验证。适用 #hl[高质量/高可靠]；不适用功能简单、短生命周期。
+ FOM 面向功能：目标：满足需求、改功能（新功能/改进/兼容/测试）。四阶段：分析→设计→修改→测试。适用 #hl[功能复杂/长生命周期]。
+ 软件再工程：
  重构改造旧系统，提"四性"(可维护/重用/扩展/移植)，#hl[功能性能不变]。
  - *库存分析*：建系统清单(规模/修改数/文档质量/业务价值…)，定期修订。
  - *四象限*：#hl[低质量+高价值→再工程]；高质高值→正常维护；低价值→一般维护或弃。
  - *三类重构*：文档 / 代码 / 数据库。
  - *坏味道(高频)*：重复代码、过长函数、过大类、过长参数列。
#table(
  columns: (auto, auto, 1fr),
  inset: 3pt, stroke: 0.4pt + gray,
  fill: (_, r) => if r == 0 { luma(230) },
  table.header([*概念*], [*方向*], [*做什么*]),
  [逆向工程], [低→高], [由代码反推设计/需求与文档],
  [重构/重组], [同层], [改善结构，不改抽象层次],
  [正向工程], [高→低], [重建系统并集成新需求/技术],
)
#note[再工程 = 逆向(理解旧系统) + 重构/改造 + 正向(重建并增强)]

#sec[ch6软件设计]

#note[
  软件设计是一个决策的过程。
  
  目标：满足用户的功能需求和质量需求以及系统本身的开发质量
  
  过程：理解需求；体系结构设计；详细设计
  
  原则：模块化、单一职责、高内聚低耦合、开闭原则
]

#note[
  软件体系结构是能够支持涉众对系统进行理解所需的一组结构，包括软件元素、元素之间的关系/协作，以及两者产生的特定属性（功能属性、质量属性）。

  模式：被实践反复证实的好的体系结构设计方案

  描述方法：上下文+问题+解决方案
]

// ===== 体系结构模式速查（压缩版，常考详写/少考略写）=====
#table(
  columns: (3.4em, 1.15fr, 1fr, 1.15fr),
  align: (x, _) => if x == 0 { center + horizon } else { left + top },
  stroke: 0.4pt + rgb("#bbb"),
  inset: 3pt,
  table.header([*模式*], [*识别（何时选）*], [*构成*], [*弱点*]),

  [*管道-\ 过滤器*],
  [固定顺序的数据流转换（编译器、日志处理）],
  [过滤器(转换,可并发)+管道(FIFO,不改数据)],
  [不适合交互式/长时运行;过滤器多→开销大],

  [*黑板*],
  [无固定流程的复杂求解(NLP/语音/CV)],
  [黑板+知识源+控制器],
  [收敛性、可预测性弱],

  [*分层*],
  [需分离可独立开发演化的部分],
  [层 + 单向 allowed-to-use 关系],
  [增层→设计成本↑、性能↓],

  [*C/S*],
  [大量客户端访问共享资源/服务],
  [客户端+服务器+请求/响应连接器],
  [服务器=性能瓶颈+单点故障;C/S 职责划分改动成本高],

  [*代理*],
  [分布式服务定位与动态绑定],
  [broker 中介(+客户端/服务端代理)],
  [增延迟、单点、瓶颈、难测试],

  [*发布-\ 订阅*],
  [生产者/消费者互不知身份、事件驱动广播],
  [发布/订阅端口+事件分发器(总线)],
  [顺序控制弱、交付不保证、延迟↑],

  [*MVC*],
  [GUI 与业务/存储分离、多视图同步状态],
  [模型+视图+控制器(控制器协调二者)],
  [对简单界面过重],

  [*P2P*],
  [平等实体互相提供/利用资源],
  [peer + 请求/响应连接器],
  [一致性/安全/可用性管理复杂],

  [*SOA*],
  [跨平台/语言/组织互操作],
  [提供者/消费者+ESB+注册表+编排;SOAP/REST],
  [中间件开销、整体性能难保证],

  [*微服务*],
  [单体痛点(臃肿/迭代慢/故障扩散);多变业务+高并发],
  [小型独立服务+轻量通信+独立部署],
  [运维/数据一致性/调用链复杂度↑],
)

#sub[任务：]软件系统的分解，满足软件系统体系结构重要需求

#sub[过程：]确定需求、初步体系结构设计、重用开源软件及已有软件资产、优化体系结构设计

#sub[归档：]写文档，为了培训、沟通、明确

#sub[设计原则：]单一职责原则、开放封闭原则（对拓展开发，对修改封闭）、里氏替换原则（父子可替换）、接口隔离原则、依赖倒置原则（抽象不依赖于细节、细节依赖抽象）

#sec[ch10软件项目管理]

#note[项目是为某个特定目标做出的一次性努力]

#sub[任务：]按约定时间交付；总成本控制在预算内；交付质量满足客户期望；保持一个有凝聚力、运作良好的开发团队。

#sub[软件规模估算:]
+ 专家评估法
+ 类比法
+ 参数法则：代码行法，功能点法

#sub[五类组件 ＋ 加权因子]
*两文件*(ILF/EIF=数据功能) ＋ *三处理*(EI/EO/EQ=事务功能)
#table(columns: (auto, 1fr, auto, auto, auto), align: (left,left,center,center,center),
  [缩写],[名称],[低],[平],[高],
  [#hl[ILF]],[内部逻辑文件],[7],[10],[15],
  [#hl[EIF]],[外部接口文件],[5],[7],[10],
  [#hl[EI]],[外部输入],[3],[4],[6],
  [#hl[EO]],[外部输出],[4],[5],[7],
  [#hl[EQ]],[外部查询],[3],[4],[6],
)
#note[*判别*：写/改数据→#hl[EI]｜只读检索→#hl[EQ]｜加工输出(报表/票据)→#hl[EO]｜内部存储→#hl[ILF]｜外部接口→#hl[EIF]]

#sub[计算流程 · 公式]
①需求 ②分解5类 ③查表加权 ④UFP ⑤定TCF ⑥FP
UFP ＝ Σ(各类个数 × 加权因子)
FP ＝ UFP × TCF
$ "TCF" = 0.65 + 0.01 times sum"DI" $
ΣDI∈[0,70] ⟹ #hl[TCF∈[0.65, 1.35]]

技术因素对软件规模的影响调整因子：*DI 0\~5*：0无影响·1偶然·2偏下·3一般·4重大·5强烈

#sub[软件项目风险：]软件开发过程中可能出现的、对项目和产品造成损失或影响的因素。

#hl[风险的类别：]

+ 需求风险：已成基准但仍变化、定义不清/不准/不一致、增加额外需求、未经用户确认
+ 人员风险：招聘超期、骨干流失（尤其项目结束前离开）、士气低/缺激励、缺乏经验工具基础、与管理层关系不佳致决策迟缓
+ 技术风险：指标达不到预期、技术方案不能实现
+ 计划风险：目标不明确、过于乐观、不具体
+ 设计/产品风险：设计低劣、高错误率模块需反复返工、为兼容/重用/性能需更多时间


#hl[风险管理的方法：]
+ 风险识别：制定常见风险列表、基于关键因素检查、请专家识别
+ 风险分析：量化：评估发生概率 × 影响程度
+ 风险排序：按风险暴露量排序，重点关注大的，小的入「待观察清单」
+ 风险管理规划：依据认知/量化/排序，确定应对策略与技术手段
+ #sub[风险处理/化解：]风险回避、风险转移、风险减小、接受风险、风险预留
+ 风险监控：定期重评概率与危害、重新排序、对上升风险采取措施

#sec[ch5需求工程]


#note[
  覆盖从项目启动到需求最终被满足的全部生命周期，包括需求获取、分析、规格化（编写文档）、验证、确认、管理（变更管理，版本管理，跟踪）等一系列可重复，有组织的工程活动
]
  
#sub[需求：]定义做什么，而不是怎么做

#sub[质量要求：]完整性（把功能描述完整）、正确性、无歧义（可量化，比如反应时间<=2s，一致性，可行性，有优先级（划分P0/P1/P2），必要性（不开发没用的），可追溯性（可以追溯到需求源头），可验证性（基本和无歧义性绑定了，可量化，几秒这种）

#sub[需求分类：]业务需求和用户需求构成系统需求，系统需求分成硬件需求和软件需求，用户需求又对软件需求产生了设计约束，软件需求又分为功能需求和非功能需求

#sub[需求工程过程：]需求获取到需求建模与分析（整合获取信息，发现矛盾/冲突，协商解决，得到涉众（利益相关者）共同认可的需求，到需求确认与验证，到需求规约（文档化）

#sub[需求工程过程模型：]瀑布模型，迭代模型（反馈，瀑布+有问题就回溯），增量（拆子需求，用瀑布/迭代）

#sub[需求获取任务：]确定需求开发计划，确定项目的目标和范围，确定调查对象，实地收集需求信息，确定非功能需求

#sub[需求获取方法：]访谈法，研讨会法/焦点小组，问卷调查法，观察法，基于视角的阅读（分析业务资料）

#sub[需求规约五原则：]规约应对其所属的系统进行描述，规约必须局部化和解耦，功能与实现相分离（说做什么，不说怎么做），使用面向处理的说明语言描述功能需求，规约必须容许不完整性并允许扩充

#sub[需求规约八特性：]正确性、无歧义、完备性、一致性、重要性/稳定性、可验证性、可修改性、可追踪性。

#sub[需求确认方法：]用户评审、原型验证、需求测试、现场试用。

#sub[需求验证方法：]桌面检查法、走查法、审查法。

#sec[ch7编码实现]

高质量的代码具备健壮性，高性能，可读性，可扩展性，可移植性，可重用性和可测试性

代码重用

常用的定位程序缺陷的方法有直接定位法，回溯法，排除法和辅助工具定位法

#sub[程序的改进空间：]
+ 正确性错误
+ 健壮性差
+ 职责不单一
+ 冗余与不一致
+ 时间复杂度不优
